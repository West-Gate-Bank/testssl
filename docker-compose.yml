services:
  testssl:
    image: ghcr.io/west-gate-bank/testssl:ubi
    container_name: testssl
    restart: always
    environment:
      # ip address or fqdn of the email server and port to send (no authorization support)
      - EMAIL_SERVER=10.1.0.11:25
      # email rewrite domain on who the email is from
      - EMAIL_DOMAIN=westgate.bank
      - TZ=America/Chicago
      - RECIPIENT_TO="cchapman@westgate.bank"
      - RECIPIENT_FROM="testssl@westgate.bank"
      - SUBJECT="Monthly SSL Report"

    volumes:
      # I have example files in the repo for email.txt domains.txt emailaddres.txt and mycron
      - ./domains.txt:/data/domains.txt:ro
